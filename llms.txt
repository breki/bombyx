# bombyx

> Drive isolated AI-agent VMs on a remote libvirt host
> over SSH. Rust CLI, derived from the rustbase template.

## Project Structure

Cargo workspace, CLI only:

- `crates/bombyx` — core library and CLI binary (paths below
  are `src/`-relative unless written in full)
  - `config.rs` — `Config` itself, and the two loaders: one
    for `bombyx.toml`, one for the registry
  - `config/registry.rs` — the operator's own `config.toml`:
    the VM host, and a `[projects.<name>]` table per project
  - `config/host.rs` — where the VM host name comes from, the
    ranking between the four sources, and the shape a host may
    take
  - `name.rs` — validated `ScratchName` (one path segment)
  - `remote.rs` — ssh command construction (pure)
  - `remote/probe.rs` — the read-only `doctor` probe scripts
  - `vagrantfile.rs` — renders the Vagrantfile, embeds the
    bootstrap script
  - `plan.rs` — `Action` → ordered command list
  - `doctor.rs` — the check model; submodules own the rest
    - `doctor/probes.rs` — which probes run, reading a result
    - `doctor/readonly.rs` — proving a probe script writes nothing
    - `doctor/text.rs` — host text made safe to print, clipping
    - `doctor/local.rs` — the workstation-side checks
    - `doctor/report.rs` — collecting and rendering findings
  - `tool.rs` — `PATH` lookup, never the working directory
  - `bin/bombyx/main.rs` — clap parsing, host-provenance
    reporting, process spawning, the self-update ordering
  - `crates/bombyx/templates/bootstrap.sh` — the script the
    guest runs, identical for every project
- `xtask` — build automation (not published)

No web crate, no frontend, no E2E suite, no deploy
subsystem — those were removed from the template.

## Model

bombyx never manages VMs directly. It generates a
Vagrantfile and a bootstrap script from `bombyx.toml`,
writes both onto the VM host over SSH, and runs `vagrant`
there. The VM host's own filesystem holds no project file;
the guest clones the repository itself once it is running.
The workstation still holds a checkout, because
`bombyx.toml` is read from the working directory.

## Commands

```
bombyx up                 # write the generated files, boot the VM
bombyx provision          # re-run provisioning in the guest
bombyx down               # halt the VM
bombyx shell              # shell inside the VM (ssh -t)
bombyx status             # vagrant status
bombyx doctor             # check preconditions, read-only
bombyx reset              # restore fresh-install snapshot
bombyx destroy <project>  # destroy the VM, remove its dir
bombyx scratch <name>     # boot a throwaway VM
bombyx discard <name>     # destroy a throwaway VM, remove its dir
bombyx self-update        # replace the binary; needs git, curl, tar
```

`destroy` requires the project name as confirmation.
`destroy` and `discard` both `rm -rf` the host directory after
destroying the VM; `remote_root` therefore rejects `..`, and
`Config::validate` is the only check on `remote_root`;
`remote::remove_dir` carries a `debug_assert` that is a
backstop for a hand-built path, not the guard.

Global flags: `--config <path>` (default `bombyx.toml`),
`--host <alias>`, `--dry-run` (print the argv instead of
running it).

## Configuration

`bombyx.toml` in the project root. Committed, and carries
**no `host`** -- one there is an error, not a warning.

`bombyx.toml.sample` in the repository root is the only full
example, and a test loads it as shipped. Keys:

- `project` — required; VM name and directory on the host
- `[vm]` — required, no defaults: `provider`, `box`, `cpus`,
  `memory`. Use `libvirt`; `hyperv` is written but bombyx does
  not tell vagrant to select it (`docs/todo.md`)
- `[source]` — required: `repo`, `ref`, `script`, what the
  guest clones and runs
- `remote_root` — optional, `~/vms`. Must sit above the tables:
  a bare key after `[source]` binds to that table

The VM host belongs to the developer, not the project: each
person has their own hardware on their own network, so a
committed value is wrong for everyone but its author -- and
`destroy` runs `rm -rf` on whatever host is in force. It is
resolved from `--host`, `BOMBYX_HOST` and a per-developer
`config.toml` -- called **the registry** from here on, because
it also carries a table per project -- in that order; see
"Where bombyx looks for the host" in `README.md` for the
authoritative list, and `resolve_host` in `config/host.rs` for
the code.

`resolve_host` ranks a fourth source the CLI cannot reach: a
`host` key inside a `[projects.<name>]` table, between
`BOMBYX_HOST` and the file-wide `host`. `HostSources.project` is
`None` on every path today, so the key never wins. The README
section above says the same and is where the count is kept.

Every `host` in the registry is checked while the file is being
read, by `config::registry::parse` -- the file-wide key and
every project's, not only the one a command wants. A host value
is `ssh`'s first positional argument, and this is the file the
operator writes host names in, so a value bombyx would refuse is
reported while they are looking at that file. Holding a
`Registry` is the proof every host in it passed;
`Registry::host` and `Registry::project_host` therefore run no
rule of their own, and `Project::validate` does not check
`host`. `--host` and `BOMBYX_HOST` are checked separately, by
`check_winning_host` on whichever source won. Note that `--host`
makes `resolve_host` skip reading the registry, so a bad host in
a file that run never opens goes unreported;
`Config::load_project` always reads the file and has no such
gap.

The ranking itself is `config::host::rank`, which takes the
registry already read rather than opening it. `resolve_host`
reads the file and calls it; `Config::load_project` reads the
file for the project's settings and calls it with that same
copy, so a file edited mid-run cannot supply a project host and
a file-wide host that never coexisted.

`Config::load_project(name, sources)` is the registry
counterpart of `Config::load`: it takes a project name instead
of a path to a `bombyx.toml`, reads the `[projects.<name>]`
table for every setting but the host, and returns the same
`(Config, HostOrigin)` pair. It ranks the host for the project
it loads whatever `HostSources.project` says, so the settings
and the host always come from one entry. No command calls it
yet -- `project-selection-flag` in `docs/todo.md` is the step
that switches the binary over and deletes `Config::load`.
The one error it adds is `RegistryNotFound`, for a machine with
no registry file at all; `ProjectNotFound` already existed. An
illegal project name is refused before the file is looked for,
so no message ever advises a `[projects.<name>]` heading that
the parser would reject.

`Config::load` returns an error naming the config file, plus
`--host` and `BOMBYX_HOST`, when no source supplies a host. It
takes a `HostSources` carrying the flag, the env value, the
project name and the config dir, and returns the winning
`HostOrigin`, so precedence is unit-testable without mutating
the process environment and the binary reports the winner rather
than re-deriving it; the binary is the only thing that reads
`std::env`. A config dir from the environment must be an
anchored path -- a relative one would resolve against the
working directory and take the host out of the repo.

Every host source sits outside the checkout: bombyx opens no
file in the project directory to find a host. That is a rule
about files and not about everything a repository can reach. A
per-directory environment tool (`direnv`, `mise`, a CI job)
reads its settings from inside the clone, and a
`BOMBYX_CONFIG_HOME` set that way redirects bombyx to another
`config.toml` without printing a provenance line. The
per-project `host` key described above does not close this: no
command reaches it yet, so `--host` still covers one run and
nothing else records the choice.

`remote_root` must be anchored (`/` or `~/`)
and name at least one directory below that anchor. bombyx
creates, writes into and -- on teardown -- deletes the
directory it derives from the value, so a root that resolves
to a top-level directory is refused when the config loads.

## Build Commands

```
cargo xtask validate          # the umbrella gate; see CLAUDE.md for the steps
cargo xtask check             # type-check all targets, run none
cargo xtask test [filter]     # tests only
cargo xtask clippy            # lint only
cargo xtask doc               # doc build + doc-link check
cargo xtask fmt               # format code
cargo xtask coverage          # coverage check (>=90% workspace, >=85% per module)
cargo xtask dupes             # duplication check (<=6%)
cargo xtask audit             # RUSTSEC advisories
```

`cargo xtask validate` is the canonical gate (`CLAUDE.md`
under Definition of Done lists the steps in run order);
`/release` runs it as the release gate; `/commit` does not run
it at all. A Claude Code Stop hook
(`.claude/hooks/stop-check.sh`) runs a faster subset -- fmt,
clippy, doc, test -- skipping coverage and duplication. Never use raw
`cargo test` / `cargo clippy`.

## Conventions

- Rust edition 2024, 80-column code and markdown
- `thiserror` in the library, `anyhow` in the binary
- `#[forbid(unsafe_code)]`, clippy pedantic, zero warnings
- Logic lives in the library: `src/bin/` is excluded from
  coverage, so code there ships untested
- `remote.rs` builds commands but never spawns processes,
  which is what keeps it testable without a VM host
- `bombyx.toml` ships inside a repo, so it is untrusted:
  every field is validated against an allowlist. `host`
  reaching `ssh` as `-oProxyCommand=...` would be local RCE
- Remote paths go through `quote_remote_path`, which leaves
  a leading `~` outside the quotes — a POSIX shell does not
  expand `~` inside single quotes

## Claude Code Skills

| Skill | Purpose |
|-------|---------|
| `/check` | Type-check all targets, running none |
| `/test` | Run tests with agent-friendly output |
| `/validate` | Full quality pipeline |
| `/commit` | Commit with diary and CHANGELOG (no reviewing) |
| `/review` | Review uncommitted work; commits nothing |
| `/review2` | The same reviewers, one at a time |
| `/release` | Cut a SemVer release |
| `/retrospect` | Workflow retrospective |
| `/todo` | Capture a work item into `docs/todo.md` |
| `/implement` | Plan + execute a captured item |
| `/issue` | Work a GitHub issue end to end |
| `/architect` | Architecture guide |
| `/short` | Restate the reply above in under 40 words |
| `/template-sync` | Pull upstream rustbase improvements |
| `/template-improve` | Log feedback about the template |

## Origin

Derived from https://github.com/breki/rustbase — see
`.template-sync.toml` for the pinned commit.
