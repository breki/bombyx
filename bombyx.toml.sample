# Sample bombyx project configuration.
#
# Copy this to `bombyx.toml` in the ROOT OF THE PROJECT you
# want a VM for -- not here. Each project carries its own
# config. bombyx reads the file from the directory you run it
# in and does not search upwards the way git and cargo do, so
# run bombyx from that root.
#
# Paths in the comments below, such as docs/trust-boundary.md,
# are in bombyx's own repository:
# https://github.com/breki/bombyx
#
# THERE IS NO `host` KEY HERE, AND ONE IS REFUSED.
# Which machine runs the VMs is a property of whoever is
# driving bombyx, not of the project: everyone has their own
# hardware on their own network. See the bottom of this file.

# Project name. Doubles as the VM's directory name on the
# host, e.g. ~/vms/myproject.
project = "myproject"

# Optional; `~/vms` if you leave it out.
#
# Root on the VM host under which project directories are
# created. Scratch VMs land in
# <remote_root>/scratch/<project>/<name>, so the same scratch
# name in two projects cannot collide.
#
# Must start with `/` or `~/`, or be exactly `~`, and must name
# at least 1 directory below that anchor, with no `.` or `..`
# segment. (`~vms` is refused: the `~` has to be the whole
# first segment.) bombyx
# creates, writes into and -- on teardown -- deletes the
# directory it derives from this value, so a root like `/`, `~`
# or `~/.` is refused when the config loads.
#
# A leading `~` is expanded by the remote shell; anywhere else
# it is rejected, as are shell metacharacters.
remote_root = "~/vms"

# EVERY KEY BELOW A TABLE HEADER BELONGS TO THAT TABLE.
# `remote_root` sits above `[vm]` for that reason: written
# after `[source]` it would parse as `source.remote_root` and
# the whole file would be refused.

# bombyx builds the machine from this table. All four keys are
# required: bombyx cannot guess a base image, and any size it
# picked would be wrong on either a laptop or a workstation.
[vm]
# Only libvirt has ever been exercised. The Hyper-V settings
# were written from the provider's documentation, not from a run.
provider = "libvirt"          # libvirt or hyperv
box = "generic/ubuntu2204"
cpus = 4
memory = 8192                 # MiB

# The guest clones this repository once it is running. All
# three keys are required. bombyx sends no project file
# anywhere: the VM fetches the repository itself and runs
# `script` out of its own clone.
#
# A private repository needs a credential inside the guest, and
# code in there can read it. See docs/trust-boundary.md.
[source]
repo = "https://github.com/you/myproject"
ref = "main"
script = "vagrant/provision.sh"   # path inside the clone

# WHERE THE VM HOST GOES INSTEAD
#
# Set it once, in your own file outside any repo:
#
#   Linux/macOS  ~/.config/bombyx/config.toml
#   Windows      %APPDATA%\bombyx\config.toml
#
#   # config.toml
#   host = "my-vmhost"
#
# `host` is an SSH alias, resolved through your ~/.ssh/config,
# so bombyx never handles addresses, usernames or keys.
#
# Three other sources override that when you need them --
# `--host`, `BOMBYX_HOST`, and a gitignored `bombyx.local.toml`
# beside this file, which can also override any field above.
# "Where bombyx looks for the host" in bombyx's own README
# gives the full order; it is deliberately documented in one
# place.
